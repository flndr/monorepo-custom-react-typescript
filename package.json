{
    "private": true,
    "workspaces": [
        "apps/*",
        "lib/*"
    ],
    "name": "monorepo-custom-react-typescript",
    "scripts" : {
        "clean" : "rimraf ./**/node_modules",
        "cleaninstall" : "yarn clean && yarn"
    },
    "devDependencies": {
        "lerna": "^3.20.2"
    },
    "dependencies": {
        "rimraf": "^3.0.0"
    }
}
